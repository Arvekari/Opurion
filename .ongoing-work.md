# Ongoing Work Log (Local, Ignored)

Purpose: keep interruption-safe notes about current unfinished execution work.

## How to use

- Update this file before/after significant changes.
- Keep entries short and action-oriented.
- Add exact commands used for validation.
- Record blockers and next concrete step.
- Mark each task with status: `PARTIAL`, `TODO`, or `BLOCKED` (completed work goes to `changelog.md`).
- Group and prioritize unfinished work.
- n8n is for development orchestration only (not product runtime behavior). when creating workflow it should be tested fully that it reports executions and works end to end error in work flow goes to P0 priority!
- Any n8n workflows created for dev cycles must be listed in this file with Name, ID, and Purpose.
- Use `pnpm run ongoing:bridge` outputs as the interface for feeding ongoing-work objectives into n8n automation.

## Autonomous Execution Rules

Agent may continue automatically without confirmation when:

- Task is a UI migration
- Task is refactor only
- No database schema changes
- No API contract changes
- No destructive file deletions

If these conditions are met:
→ Do NOT ask for confirmation
→ Continue execution until task acceptance criteria are met.

Human confirmation is required only for:

- at moment there is no situation where you should need human intervention

## Status Legend

- `PARTIAL`: started, needs more work.
- `TODO`: not started.
- `BLOCKED`: cannot continue without external action.

## Live Update Protocol (Always-on)

- Update at each phase change: `investigating` â†’ `editing` â†’ `validating` â†’ `committing` â†’ `pushing`.
- Completed implementation items must be recorded in `changelog.md` first, then removed from this file.
- Keep only unfinished, in-flight context here.
- When one unfinished task reaches completion, immediately move to next highest-priority unfinished task.
- If interrupted, always update:
  - `Current step now:`
  - `Last command run:`
  - `Next exact command:`
- For orchestrated execution, every open task (`PARTIAL`/`TODO`/`BLOCKED`) must be reported to orchestration so internal task tables stay complete.
- For n8n linkage, every open task line must start with a stable task-id marker in this format: `[taskId: <id>]`.
- n8n task table contract: `taskId` in Data Table must match `[taskId: ...]` in this file for the same objective.
- Any user-added item under `## Uncategorized and not yet id:t TODO Work` must be auto-normalized into prioritized `P0`-`P5` with a generated `[taskId: ...]` (default target priority: `P0`).
- Any issue found during cleanup/validation (lint, tests, typecheck, orchestration sync, release flow) must be opened immediately as `P0` with `[taskId: ...]` until resolved.
- Commit/push release gate:
  - Default: commit/push only after all open tasks in current release batch are done.
  - Exception: if there is an active P0 incident, commit/push may happen earlier for urgent stabilization.

## Orchestration Enforcement

- Mode: AUTO
- Exception: OFF
- ConfirmationRequired: OFF
- ExceptionReason:

## Prioritized Unfinished Work

### P0 (active now)

- `TODO` None.

### P1 (active now)

- `TODO` None.

### P2 (active now)

- `TODO` None.

### P3 (active now)

- `TODO` None.

### P4 (active now)

- `TODO` None.

### P5 (active now)

- `TODO` None.

## BLOCKED

- `BLOCKED` None currently.

## Upcoming Work (_give id and priorisation_) (Uncategorized and not yet id:t TODO Work, cannot be removed, need to be formatted correctly!)

- `TODO` None.

## Strategic Gap Matrix References

### 2026-03-17 — Opurion self-hosted gap matrix

| Area                                  | Current likely state                               | Missing                                                                                                                    | Priority | Why it matters                                                            |
| ------------------------------------- | -------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------- |
| Installation and deployment           | Likely technically deployable by experienced users | One-command install, polished Docker Compose, Helm chart, K8s manifests, upgrade path, migration flow, backup/restore docs | P1       | In self-hosted products, install quality is the equivalent of SaaS polish |
| Configuration management              | Likely env/config-file driven                      | Central config UI, validation, config wizard, safe defaults, environment profiles                                          | P1       | Reduces setup friction and operator mistakes                              |
| Private-cloud reference architectures | Likely conceptual or partial                       | Small / medium / enterprise deployment blueprints, HA design, reverse proxy patterns, storage guidance, scaling model      | P1       | Companies need confidence that the platform is production-deployable      |
| Identity and access management        | JWT/RBAC direction exists                          | SAML, OIDC, LDAP/AD, service accounts, API tokens, team/workspace isolation, fine-grained roles                            | P1       | Enterprise private installs live or die on IAM maturity                   |
| Admin / operator console              | Likely limited or early                            | Central admin UI for users, tenants, providers, quotas, audit, retention, storage, health                                  | P1       | Self-hosted needs a real operator experience, not just config files       |
| Secrets management                    | Security direction exists                          | Secure secret vault integration, rotation support, separation of platform/admin/user secrets, secret auditability          | P1       | Essential for regulated or security-conscious deployments                 |
| Audit logging                         | Likely planned or partial                          | Full audit trail for logins, config changes, key changes, project access, sharing, admin actions                           | P1       | Private-cloud buyers expect traceability and accountability               |
| Monitoring and observability          | Likely logs exist or are planned                   | Metrics, tracing, dashboards, health checks, alert hooks, Prometheus/Grafana guidance                                      | P1       | Ops teams need to see whether the platform is healthy and why it fails    |
| Backup, recovery, and rollback        | Likely DB-level possible but not productized       | Built-in backup guidance, export/import, rollback strategy, disaster recovery documentation                                | P1       | Self-hosted platforms must survive failure cleanly                        |
| Upgrade management                    | Likely manual or repo-based                        | Versioned migrations, upgrade assistant, compatibility checks, rollback-safe upgrade path                                  | P1       | Private deployments fear upgrades more than initial installs              |
| Team collaboration                    | Foundations may exist conceptually                 | Shared workspaces, project roles, comments, controlled sharing, approvals, artifact collaboration                          | P1       | Bolt/Lovable-level usefulness requires team use, not only solo use        |
| Workspace and tenant isolation        | Possibly early                                     | Hard separation between users, teams, customers, environments, and data scopes                                             | P1       | Critical for enterprise multi-team or multi-customer usage                |
| Git integration                       | Likely not fully productized                       | GitHub/GitLab import/export, branch awareness, commit history linkage, PR-friendly workflow                                | P1       | In private infra, Git integration replaces hosted publish convenience     |
| CI/CD handoff                         | Likely manual                                      | Export pipelines, templates for GitHub Actions/GitLab CI/Azure DevOps, deploy artifacts, release workflow                  | P1       | Enterprises want clean handoff to their own delivery pipeline             |
| First-run onboarding                  | Likely repo/readme oriented                        | Setup wizard, sample project, provider connection test, guided first workspace creation                                    | P1       | This is where competitors feel fast and polished                          |
| Builder UX                            | Architecture likely stronger than UX               | Better prompt-to-app flow, prompt-to-artifact flow, structured edit loop, visual previews, fix/retry UX                    | P1       | This is one of the biggest visible gaps versus Bolt/Lovable               |
| Error handling UX                     | Likely technical and developer-oriented            | Human-readable errors, recovery suggestions, diagnostics panel, retry/fix loop                                             | P1       | Product maturity is often judged by failure experience                    |
| Templates and starter packs           | May be limited                                     | Ready-made templates for chat app, internal assistant, RAG workspace, workflow tool, admin panel, docs assistant           | P1       | Templates accelerate time-to-value dramatically                           |
| Versioning and snapshots              | Possibly partial at repo level                     | Project snapshots, restore points, diff view, safe rollback, change explanations                                           | P1       | Users need confidence when AI edits or restructures work                  |
| Artifact system maturity              | Likely present directionally                       | Rich artifact lifecycle, preview, share, export, dependency visibility, versioned artifacts                                | P2       | Strong artifact handling can become a differentiator                      |
| Review and approval workflow          | Likely missing or limited                          | Human approval gates, reviewer mode, change acceptance flow, role-based approval                                           | P2       | Important for enterprise governance and safe AI-assisted work             |
| AI agent modes / personas             | Possibly generic today                             | Specialized roles such as builder, architect, reviewer, debugger, documenter, security reviewer                            | P2       | Makes the system feel more capable and structured                         |
| Repo-aware engineering assistance     | Likely weaker than IBM-style tools                 | Whole-project understanding, code review mode, architecture review, dependency awareness                                   | P2       | Needed if Opurion wants to compete with serious dev-assistant tools       |
| Security testing / policy enforcement | Security intent exists                             | Integration policies, allowed provider rules, egress controls, DLP-style safeguards, content boundaries                    | P2       | Private-cloud buyers often want controlled AI usage, not just access      |
| Data governance and retention         | Likely incomplete                                  | Retention settings, data residency controls, deletion workflow, project archival, legal hold support                       | P2       | Important in enterprise and regulated environments                        |
| Multi-provider orchestration UX       | Likely technically possible                        | Clear provider selection UX, routing logic, fallback logic, per-workspace provider policies, cost visibility               | P2       | Provider neutrality is a core Opurion strength if surfaced properly       |
| Cost and usage visibility             | Likely limited                                     | Token/cost tracking, quota controls, team usage views, provider comparison                                                 | P2       | Private installs still need budgeting and governance                      |
| Plugin / extension model              | Unknown or early                                   | Defined extension SDK, supported integration model, stable internal contracts, marketplace-like structure                  | P2       | Important for ecosystem growth and long-term extensibility                |
| Documented API and automation surface | Likely partial                                     | Stable admin API, project API, automation hooks, webhook model                                                             | P2       | Enterprises want to automate the platform itself                          |
| Performance and scale guidance        | Likely not fully productized                       | Benchmarks, sizing guide, scale thresholds, concurrency model, storage/performance tuning guide                            | P2       | Helps customers trust the platform in production                          |
| Mobile / responsive usability         | Unknown                                            | Reliable responsive admin and builder UX                                                                                   | P3       | Useful, but less important than desktop for private enterprise use        |
| Branding / visual polish              | Likely improving                                   | More premium UI consistency, visual hierarchy, empty states, guided flows, polished design language                        | P3       | Helps perception, but does not replace platform maturity                  |
| Community / ecosystem packaging       | Early                                              | Better docs portal, example repos, integration examples, deployment cookbook                                               | P3       | Helps adoption and lowers operational risk                                |
| Marketplace / sharing ecosystem       | Likely out of scope or early                       | Internal template exchange, private catalog, reusable building blocks                                                      | P3       | Nice accelerator, but not critical for first serious versions             |

## Live Session Notes

- 2026-03-11 ~12:45 — Build-mode delivery guardrail hardening (user-reported chat code dump despite build mode):
  - `app/lib/common/prompts/new-prompt.ts`: added explicit build-mode rule that implementable requests (pages/apps/features/files) must be delivered via one executable `<boltArtifact>` and not as standalone fenced code blocks in chat.
  - `app/lib/common/prompts/prompts.ts`: added matching implementation-mode rule to the legacy/original system prompt profile to keep behavior consistent across prompt profile selection.
  - `unit-tests/common/prompts-finetuned.default.test.ts` and `unit-tests/common/prompts-system.default.test.ts`: updated assertions to validate current identity/year and the new artifact-only implementation rule.

- 2026-03-11 ~12:30 — System prompt identity + standards update:
  - `app/lib/common/prompts/new-prompt.ts`: changed runtime system prompt identity from StackBlitz Bolt to `Bolt2.dyi`, credited Markku Arvekari as creator, updated year to `2026`, and added explicit senior application architect behavior requirements.
  - Added prompt-level requirements to:
    - identify as `Bolt2.dyi`
    - never attribute authorship to StackBlitz in user-facing identity responses
    - always plan security testing with OWASP-style considerations where relevant
    - place unit tests under `unit-tests/**`
    - create/update durable docs under `docs/**`
    - add concise comments for non-obvious code and architecture/security-sensitive logic
    - treat Expo / React Native work as senior-level mobile architecture work
  - Validation: source prompt file updated successfully.

- 2026-03-11 ~12:15 — P0 chat routing deep audit + v3 migration logged properly:
  - Research findings:
    - `@ai-sdk/react` is `3.0.118` and no longer exposes `append`, `reload`, `data`/`setData`, `setInput`, or `addToolResult`.
    - v3 `useChat` uses `transport`, not v2-style `api`/`body`/`streamProtocol` options.
    - Backend `/api/chat` still speaks the old v2 protocol: request messages use `content`, and streaming output is the v2 data-stream format emitted via `formatDataStreamPart(...)`.
    - Root cause: frontend had been partially upgraded to v3 while backend remained on v2 protocol, so requests and streamed responses no longer matched SDK expectations.
  - Implementation completed:
    - `app/lib/chat/boltChatTransport.ts`: added custom transport that converts v3 UI messages into backend v2 `{ role, content }` payloads and converts backend v2 data-stream chunks back into UI message chunks.
    - `app/components/chat/Chat.client.tsx`: migrated chat hook usage to v3 transport-based API, removed invalid `setData(...)` / `chatData` / `setInput(...)` usage, derived loading from `status`, and kept a compatibility `append(...)` shim for existing UI wiring.
    - `app/lib/hooks/useMessageParser.ts`: added `parts` fallback so v3 assistant/user messages without `content` still parse and render correctly.
    - `unit-tests/components/chat/Chat.client.test.tsx`: updated mocks/assertions to match v3 `sendMessage(...)` flow.
  - Validation:
    - `pnpm vitest run unit-tests/components/chat/Chat.client.test.tsx --reporter=verbose`
    - Result: `1` file passed, `3` tests passed.
    - `pnpm vitest run`
    - Result: `322` files passed, `690` tests passed, `134` todo; `2` failed suites remain due to pre-existing `.ts` test files with JSX parse errors (`unit-tests/components/chat/Chat.client.test.ts`, `unit-tests/components/chat/ChatBox.test.ts`).
  - Next exact action:
    - Live runtime verification of the migrated chat path in browser and then cleanup/rename decision for the duplicate `.ts` JSX test suites.

- 2026-03-11 ~11:20 — P0 runtime chat send-path compatibility + debug persistence hardening:
  - `Chat.client.tsx`: added SDK compatibility sender (`sendViaChatApi`) that prefers `append(...)` and falls back to `sendMessage(...)` when `append` is unavailable; replaced URL-prompt and all user-send callsites to use helper.
  - `settings.ts`: debug mode now initializes from persisted storage and has `updateDebugMode(...)` writer (`isDebugEnabled`).
  - `useSettings.ts`: `enableDebugMode` now calls `updateDebugMode(...)` so debug toggle survives reload.
  - `Chat.client.test.tsx`: added regression test `falls back to sendMessage when append is unavailable`.
  - Validation:
    - `pnpm exec vitest run unit-tests/components/chat/Chat.client.test.tsx --reporter=verbose`
    - Result: chat client tests pass with 3 tests including fallback coverage.

- 2026-03-11 ~10:40 — User reported P0 regressions still active in runtime: missing workbench toggle and LLM responses not returning. Action taken: made ChatBox Workbench/Mode controls always text-visible (not icon-only), added send lifecycle logs in `Chat.client.tsx` (`sendMessage:init`, `first-append`, `append`), and continued P0 validation.
- 2026-03-11 ~10:41 — Validation update:
  - Focused chat tests pass:
    - `pnpm exec vitest run unit-tests/components/chat/Chat.client.test.tsx unit-tests/components/chat/ChatBox.test.tsx unit-tests/components/chat/ChatBox.input-regression.test.tsx --reporter=verbose`
    - Result: 3 files passed, 9 tests passed.
  - Preview e2e smoke pass:
    - `pnpm exec playwright test -c playwright.config.preview.ts tests/preview/chat-send-receive.smoke.spec.ts --project=chromium --reporter=line`
    - Result: 1 passed.
  - 2026-03-11 ~10:45 — UI placement correction per user request: removed Workbench toggle from chat input toolbar (`ChatBox.tsx`), added fixed Workbench toggle next to provider/model in top header (`Header.tsx`), and removed duplicate right-side header button (`HeaderActionButtons.client.tsx`).
  - 2026-03-11 ~10:47 — Confirmed top-bar-only Workbench placement: chat box no longer renders Workbench toggle; header center toggle remains next to provider/model selectors. Validation: `pnpm exec vitest run unit-tests/components/chat/ChatBox.test.tsx unit-tests/components/chat/ChatBox.input-regression.test.tsx --reporter=verbose` (2 files, 6 tests passed).
  - 2026-03-11 ~10:58 — P0 chat flow hardening + debug preferences update:
    - `Chat.client.tsx`: wrapped all `append(...)` calls with awaited try/catch + explicit `logStore.logError(...)` on failures to surface routing/send failures in UI debug logs.
    - `Messages.client.tsx`: debug button/panel now gated by debug-mode setting (`isDebugMode`), so visibility is controlled from Preferences.
    - `FeaturesTab.tsx`: added slider `Show Chat Debug Button` (uses `enableDebugMode`) under Core Features.
    - Validation: `pnpm exec vitest run unit-tests/components/chat/Chat.client.test.tsx unit-tests/components/chat/ChatBox.test.tsx unit-tests/components/chat/ChatBox.input-regression.test.tsx --reporter=verbose` (3 files, 8 tests passed).
  - 2026-03-11 ~11:05 — Additional P0 fix for user-reported "send requested but no user/LLM messages": added stuck-loading recovery in `Chat.client.tsx` so when `isLoading`/`fakeLoading` is stale with empty messages, send path force-clears loading (`stop`, `setFakeLoading(false)`, `setIsStalledStream(false)`, `setData(undefined)`) and proceeds with new send; logs recovery event `sendMessage:recover-stuck-loading`.

- 2026-03-10 14:00 — Ongoing log cleanup completed: moved completed work tracking to `changelog.md` and reset active priority queues to unfinished-only placeholders.
- 2026-03-10 ~16:00 — UI redesign batch completed (sidebar logo, collapsed icon-nav, profile initials, vertical theme toggle, Workbench header button, landing card). Changelog updated. Three open items logged as P2/P3: settings color token migration (ux-settings-colors), Docker build+run (docker-build-test), git commit (git-commit-ux), and collapsed sidebar System theme button (ux-theme-system-btn). None of this batch reached n8n orchestrator.
- 2026-03-10 ~17:00 — Completed: settings color token migration (`ux-settings-colors`) across all 4 `@settings/core/` files — zero hardcoded gray/white tokens remain. Completed: System theme button (`ux-theme-system-btn`) added to collapsed sidebar. Changelog updated for both. Remaining open: `docker-build-test`, `git-commit-ux`.
- 2026-03-10 ~12:06Z — Orchestrator sync executed: `pnpm run -s n8n:sync-open-tasks` (rowsSynced=2, openObjectives=2).
- 2026-03-10 ~12:09Z — Docker startup smoke completed: `pnpm run -s smoke:docker-startup` (build + Docker image build via `docs/docker/composed/Dockerfile` + container startup log validation passed).
- 2026-03-10 ~12:10Z — Verified UX commit already exists in git history: `64533e4 feat(ui): complete sidebar/header/landing redesign and settings color token migration`.
- 2026-03-10 ~12:14Z — n8n sync regression detected: Data Table row-write path rejected with 400 (`open_tasks_row_write_failed`), sync fell back to `bolt.work/n8n/open-tasks-table.json`.
- 2026-03-10 ~12:15Z — Resolved n8n empty-queue sync regression by skipping empty row POST in `scripts/n8n-dev-orchestrator.mjs`; validation: `pnpm run -s n8n:sync-open-tasks` now writes successfully via `/api/v1/data-tables/WwKV0LTmzJj87mm0/rows`.
- 2026-03-13 ~13:27 — Created rollback snapshot before iterative LLM reliability loop:
  - `bolt.work/ongoing-work-backups/llm-regression-20260313-132740`
  - Backed up: `app/routes/api.chat.ts`, `app/lib/.server/llm/stream-text.ts`, `app/lib/.server/llm/ollama-response-normalization.ts`, and targeted regression tests.
- 2026-03-13 ~13:31 — Completed local+cloud build-mode stabilization loop:
  - Added bounded multi-round Ollama executable recovery in `app/routes/api.chat.ts` for split-prompt/small-model narrative failures.
  - Added build-mode execution contract in `app/lib/.server/llm/stream-text.ts` so build mode keeps executable-output requirements after prompt compaction.
  - Added provider-agnostic build fallback + project-essentials completion in `app/routes/api.chat.ts` and `app/lib/.server/llm/ollama-response-normalization.ts`.
  - Existing partial React artifacts now auto-complete missing essentials (`/package.json`, `/index.html`, start command) for professional runnable output.
  - Added/updated regression coverage in:
    - `unit-tests/routes/api.chat.streaming-e2e.test.ts`
    - `unit-tests/lib/.server/llm/ollama-response-normalization.test.ts`
  - Validation passed:
    - `pnpm vitest run unit-tests/routes/api.chat.streaming-e2e.test.ts unit-tests/lib/.server/llm/ollama-response-normalization.test.ts`
    - `pnpm typecheck`
- 2026-03-13 ~13:47 — Logging discipline checkpoint (before edits):
  - User requested guaranteed before/after updates for `.ongoing-work.md` and `changelog.md` on every loop.
  - Action: writing pre-edit state now before additional code/test iterations.
- 2026-03-13 ~13:49 — Logging discipline checkpoint (after edits):
  - `.ongoing-work.md` updated before edits and again after edits in same cycle.
  - `changelog.md` Unreleased updated with continuous synchronization policy note.
- Current step now: logging sync cycle complete; ready to continue code/test stabilization loop.
- Last command run: applied patches to `.ongoing-work.md` and `changelog.md` for before/after update enforcement.
- Next exact command: `pnpm vitest run unit-tests/routes/api.chat.streaming-e2e.test.ts unit-tests/lib/.server/llm/ollama-response-normalization.test.ts unit-tests/lib/runtime/runtime.message-parser.ollama-regressions.test.ts`.

- 2026-03-13 ~13:56 — Pre-edit checkpoint for next stabilization loop:
  - Target fixes: pin provider/model during Ollama recovery rounds and tighten executable-output gating so start-only artifacts cannot pass as final output without synthesized files.
  - Scope: `app/routes/api.chat.ts`, `app/lib/.server/llm/stream-text.ts`, and focused e2e/unit regression tests.
- Current step now: applying provider-pinning + gating patches before validation run.
- Last command run: synchronization update patches for `.ongoing-work.md` and `changelog.md`.
- Next exact command: `pnpm vitest run unit-tests/routes/api.chat.streaming-e2e.test.ts unit-tests/lib/.server/llm/ollama-response-normalization.test.ts`.

- 2026-03-13 ~13:50 — Validation checkpoint (failed typecheck, remediation in-progress):
  - Focused regression tests passed (`31/31`), but `pnpm typecheck` failed.
  - Root causes:
    1. TS narrowing warning in `app/lib/.server/llm/stream-text.ts` (`modelDetails` possibly undefined in pinned-model log block).
    2. Backup snapshot `.ts` files under `bolt.work/ongoing-work-backups/llm-stabilization-20260313-134428` were compiled by `tsc`.
- Current step now: apply TS guard fix and rename backup snapshot TypeScript files to non-compiled extensions.
- Last command run: `pnpm vitest run ...; pnpm typecheck`.
- Next exact command: `Get-ChildItem bolt.work/ongoing-work-backups/llm-stabilization-20260313-134428 -Recurse -File -Include *.ts,*.tsx | Rename-Item -NewName { $_.Name + '.bak' }`.

- 2026-03-13 ~13:52 — Post-edit checkpoint for autonomous loop (completed):
  - Implemented provider/model pinning for all `streamText(...)` calls in `api.chat` main + continuation + recovery-round paths to prevent drift from Ollama to cloud providers during retry loops.
  - Added build-output pass-through gating so raw retry text is emitted only when it contains executable file-level output (file actions, HTML doc, or recognized fenced code), avoiding acceptance of weak/non-runnable artifact-like responses.
  - Added regression assertion in `unit-tests/routes/api.chat.streaming-e2e.test.ts` verifying forced provider/model pinning across initial and retry calls.
  - Resolved typecheck regression by guarding pinned-model logging and excluding backup snapshot `.ts` files from compiler scope (`.ts.bak`).
  - Validation passed:
    - `pnpm vitest run unit-tests/routes/api.chat.streaming-e2e.test.ts unit-tests/lib/.server/llm/ollama-response-normalization.test.ts unit-tests/lib/runtime/runtime.message-parser.ollama-regressions.test.ts`
    - `pnpm typecheck`

- 2026-03-13 ~16:12 — Pre-edit checkpoint for full-suite closeout:
  - Broad Vitest summary showed a single remaining failed assertion in `unit-tests/common/system-prompt-profiles.test.ts`.
  - Root cause: test still expected custom prompt profile eligibility to start at `16B`, but runtime policy was intentionally changed to `14B` when filtering out `13B` and below.
- Current step now: patch stale prompt-profile threshold assertion and rerun full unit suite.
- Last command run: full Vitest JSON summary inspection via `.tmp-vitest-summary.json`.
- Next exact command: `pnpm vitest run --reporter=json --outputFile .tmp-vitest-summary.json`.

- 2026-03-13 ~16:18 — Post-edit checkpoint for full-suite closeout (completed):

- 2026-03-13 ~16:30 — Pre-edit checkpoint for runtime bug-fix batch (3 confirmed bugs):
  - Bug 1 (path doubling / preview fails): `normalizeWorkspaceRelativeDirectory('/home/project')` strips leading slash and returns `'home/project'` instead of `''`. `cd home/project` from CWD `/home/project` resolves to `/home/project/home/project`. `#validateShellCommand` finds no `package.json` there and throws `ActionCommandError: Missing package.json`. Visible as "no preview available" and "Failed To Execute Shell Command" error alert. Fix: `app/utils/projectCommands.ts`.
  - Bug 2 (execution queue corruption / "chat resets"): `workbenchStore.#globalExecutionQueue = ...then(() => callback())` has no `.catch()`. Any thrown error from the queue (e.g. unexpected `unreachable()`) permanently rejects the queue; all subsequent `addAction` / `runAction` callbacks silently skip. Makes round 2+ LLM file actions never run. Fix: `app/lib/stores/workbench.ts`.
  - Bug 3 (build mode full-buffers cloud/OpenAI → no streaming progress): `shouldBufferCurrentBuildText = chatMode === 'build'` is always true, silencing ALL text-delta parts for cloud providers until the full response is assembled. 44+ second silence for users. Same bug in continuation segment. Fix: scope buffering to Ollama only; for cloud, stream text deltas through and append only supplemental missing parts at the end. Files: `app/routes/api.chat.ts`.
- Current step now: applying fixes.
- Last command run: full Vitest suite + tsc (both passing).
- Next exact command: `pnpm vitest run unit-tests/routes/api.chat.streaming-e2e.test.ts unit-tests/lib/.server/llm/ollama-response-normalization.test.ts unit-tests/lib/runtime/runtime.message-parser.ollama-regressions.test.ts unit-tests/utils/projectCommands.test.ts`.
  - Updated `unit-tests/common/system-prompt-profiles.test.ts` so prompt-profile eligibility assertions now match the live `14B` minimum policy.
  - Full validation passed:
    - `pnpm vitest run --reporter=json --outputFile .tmp-vitest-summary.json`
      - Result: `995` suites passed, `752` tests passed, `0` failed, `161` todo.
    - `pnpm tsc --noEmit`
      - Result: passed.
- Current step now: stabilization loop complete; logs synchronized and ready for next user task.
- Last command run: `pnpm tsc --noEmit`.
- Next exact command: none.

- 2026-03-13 ~16:09 — Pre-edit checkpoint for auto-preview reliability hardening:
  - User reported auto-preview still not working consistently and explicitly requested PHP/http-server style preview verification as well.
  - Root cause found in `app/utils/projectCommands.ts`:
    - no PHP project detection branch existed
    - plain static preview relied on `npx serve`, which is less deterministic than a built-in runtime server
    - static detection did not prefer nested entry directories explicitly
  - Runtime confirmation: `start` actions in `app/lib/runtime/action-runner.ts` do not go through shell `package.json` validation, so a built-in Node preview server can be used safely for non-Node projects.

- 2026-03-13 ~16:11 — Post-edit checkpoint for auto-preview reliability hardening (completed):
  - Completed:
    - Replaced static-site preview inference with a built-in Node HTTP server command in `app/utils/projectCommands.ts`.
    - Added PHP project detection with a PHP-template fallback preview server that strips PHP blocks and serves the remaining HTML shell, marking responses with `X-Bolt-Preview-Mode: php-static-fallback`.
    - Added nested static entry detection so `/public/index.html` and similar layouts launch preview from the correct directory.
    - Added regression coverage in `unit-tests/utils/projectCommands.test.ts` for built-in static preview, nested static directory handling, and PHP fallback detection.
    - Performed live smoke verification of the PHP fallback server outside the unit tests:
      - Result: HTTP `200`, header `X-Bolt-Preview-Mode=php-static-fallback`, body rendered expected HTML shell content.
  - Validation passed:
    - `pnpm vitest run unit-tests/utils/projectCommands.test.ts unit-tests/utils/projectCommands.edge.test.ts`
      - Result: `2` files passed, `16` tests passed.
    - `pnpm tsc --noEmit`
    - `pnpm vitest run unit-tests/components/chat/Chat.client.test.tsx unit-tests/routes/api.chat.streaming-e2e.test.ts unit-tests/lib/.server/llm/ollama-response-normalization.test.ts`
      - Result: `3` files passed, `38` tests passed.
- Current step now: auto-preview fallback hardening complete and validated, including PHP/http fallback smoke verification.
- Last command run: `pnpm vitest run unit-tests/components/chat/Chat.client.test.tsx unit-tests/routes/api.chat.streaming-e2e.test.ts unit-tests/lib/.server/llm/ollama-response-normalization.test.ts`.
- Next exact command: none.

- 2026-03-13 ~16:00 — Pre-edit checkpoint for project creation enrichment:
  - User request: project creation must support attaching shared files and guides, and those shared resources must be available to other users through database-backed project sharing.
  - Chosen implementation: persist project guides/materials/reference files as project-scoped artifact records instead of altering the base project table schema, so existing project membership/sharing immediately governs access.
  - Target scope:
    - `app/components/sidebar/ProjectsWorkspacePanel.tsx`
    - `app/components/sidebar/ArtifactsMarketplacePanel.tsx`
    - `app/components/chat/Chat.client.tsx`
    - `app/lib/stores/collab.ts`
    - targeted chat regression test coverage

- 2026-03-13 ~16:08 — Post-edit checkpoint for project creation enrichment (completed):
  - Completed:
    - `ProjectsWorkspacePanel` now supports project creation with guide text + attached shared text/spec/code files.
    - Shared project narratives/materials/guides/files now persist in the database as project-scoped artifact records, so shared users can access the same resources.
    - Project sharing controls were restored in the new Projects workspace panel.
    - Chat context injection now includes project guides and attached shared reference files in addition to project narratives/materials and discussion index.
    - Artifact marketplace now hides project-internal system records so only reusable user-facing artifacts appear there.
    - Added focused regression coverage in `unit-tests/components/chat/Chat.client.test.tsx` for project guides/files context injection.
  - Validation passed:
    - `pnpm tsc --noEmit`
    - `pnpm vitest run unit-tests/components/chat/Chat.client.test.tsx unit-tests/routes/api.chat.streaming-e2e.test.ts unit-tests/lib/.server/llm/ollama-response-normalization.test.ts`
      - Result: `3` files passed, `38` tests passed.
- Current step now: project creation enrichment complete and validated.
- Last command run: `pnpm vitest run unit-tests/components/chat/Chat.client.test.tsx unit-tests/routes/api.chat.streaming-e2e.test.ts unit-tests/lib/.server/llm/ollama-response-normalization.test.ts`.
- Next exact command: none.

- 2026-03-13 ~15:44 — Pre-edit checkpoint for sidebar collaboration UX expansion:
  - User request: make left `Projects` a real project-folder workflow where shared narratives/materials apply to all discussions under the project, and discussions can reference each other (e.g. "discussion 1" in discussion 2).
  - User request: make `Artifacts` a reusable web-shop style library (visual cards with preview/description/info) where reusable modules/components/snippets can be stored and reused.
  - Planned implementation scope:
    - Add real project/discussion workspace panel in sidebar and wire to existing collab APIs.
    - Add artifact marketplace panel wired to `/api/collab/artifacts` with create/list/copy workflow.
    - Inject selected project shared context + discussion index into outgoing chat messages for cross-discussion awareness.

- 2026-03-13 ~15:48 — Post-edit checkpoint for sidebar collaboration UX expansion (completed):
  - Completed:
    - Added `ProjectsWorkspacePanel` and integrated it into left sidebar `Projects` section.
      - Supports create/select project folder, create/select discussions, and persistent shared project narratives/materials.
    - Added `ArtifactsMarketplacePanel` and integrated it into left sidebar `Artifacts` section.
      - Supports reusable artifact creation and marketplace-style card browsing with preview image URL, type/visibility metadata, search/filter, and one-click content copy.
    - Extended `collabStore` with project context + discussion index state and setters.
    - Updated chat send-path (`Chat.client.tsx`) to prepend active project narratives/materials/discussion index into message payload so discussions can reference each other contextually.
  - Files added:
    - `app/components/sidebar/ProjectsWorkspacePanel.tsx`
    - `app/components/sidebar/ArtifactsMarketplacePanel.tsx`
  - Files updated:
    - `app/components/sidebar/Menu.client.tsx`
    - `app/components/chat/Chat.client.tsx`
    - `app/lib/stores/collab.ts`
  - Validation passed:
    - `pnpm tsc --noEmit`
    - `pnpm vitest run unit-tests/components/chat/Chat.client.test.tsx unit-tests/routes/api.chat.streaming-e2e.test.ts unit-tests/lib/.server/llm/ollama-response-normalization.test.ts`
      - Result: `3` files passed, `37` tests passed.
- Current step now: requested Projects/Artifacts UX and cross-discussion context behavior implemented and validated.
- Last command run: `pnpm vitest run unit-tests/components/chat/Chat.client.test.tsx unit-tests/routes/api.chat.streaming-e2e.test.ts unit-tests/lib/.server/llm/ollama-response-normalization.test.ts`.
- Next exact command: none.

- 2026-03-13 ~14:10 — Pre-edit checkpoint for initial-response regression loop:
  - User-reported runtime regressions still active on first response for both Ollama and cloud build mode.
  - Active failure shapes:
    1. Ollama/deepseek initial answer can collapse to a trivial Vite `/index.html` shell while the substantive implementation text leaks into chat.
    2. JSON-wrapped recovery artifacts are still being normalized as raw `/index.html` file content instead of extracting inner actions/content.
    3. Cloud/OpenAI build path can return no visible reply when buffered/fallback execution output is not emitted consistently.
  - Target scope now: `app/lib/.server/llm/ollama-response-normalization.ts`, `app/routes/api.chat.ts`, and focused streaming/normalization regression tests.
- Current step now: patch initial build-mode buffering + artifact extraction for both Ollama and cloud providers.
- Last command run: code inspection of `api.chat.ts`, `stream-text.ts`, `ollama-response-normalization.ts`, and focused tests.
- Next exact command: `pnpm vitest run unit-tests/routes/api.chat.streaming-e2e.test.ts unit-tests/lib/.server/llm/ollama-response-normalization.test.ts unit-tests/lib/runtime/runtime.message-parser.ollama-regressions.test.ts`
- 2026-03-13 ~15:05 — Pre-edit checkpoint for quality-equivalence recovery:
  - User clarified the main objective: smaller/local models may take more recovery rounds, but final build-mode output quality must converge to the same runnable standard instead of accepting weaker first executable artifacts.
  - New target fixes:

- 2026-03-13 ~15:40 — Pre-edit checkpoint for UX + chat reliability follow-up:
  - User request: make generated pages more "digital transformation" style with richer informational content, hide the debug panel in chat UI, and address OpenAI continuation/send no-op behavior observed with transient network errors.
  - Planned scope:
    - `app/components/chat/Messages.client.tsx` (remove debug panel/button rendering from chat surface)
    - `app/components/chat/Chat.client.tsx` (add one-time retry for transient network send failures)
    - `app/lib/common/prompts/new-prompt.ts` (increase content density + digital-transformation layout guidance for page generation)
  - Validation plan:
    - `pnpm tsc --noEmit`
    - `pnpm vitest run unit-tests/routes/api.chat.streaming-e2e.test.ts unit-tests/lib/.server/llm/ollama-response-normalization.test.ts`

- 2026-03-13 ~15:43 — Post-edit checkpoint for UX + chat reliability follow-up (completed):
  - Completed:
    - Hid chat debug panel controls/panel in `Messages.client.tsx` while preserving streaming status indicator.
    - Added one-time transient network retry in `Chat.client.tsx` send path (covers `TypeError: network error`/fetch-failed class failures).
    - Updated default fine-tuned prompt design guidance (`new-prompt.ts`) to favor richer digital-transformation page structures and denser informational sections.
    - Aligned cloud-streaming regression assertion in `unit-tests/routes/api.chat.streaming-e2e.test.ts` to intended progressive cloud streaming behavior.
    - Fixed queue-recovery compile issue in `app/lib/stores/workbench.ts` (`console.error` recovery logging).
  - Validation passed:
    - `pnpm tsc --noEmit`
    - `pnpm vitest run unit-tests/routes/api.chat.streaming-e2e.test.ts unit-tests/lib/.server/llm/ollama-response-normalization.test.ts`
      - Result: `2` files passed, `34` tests passed.
- Current step now: user-requested UX/reliability update completed and validated; awaiting next instruction.
- Last command run: `pnpm vitest run unit-tests/routes/api.chat.streaming-e2e.test.ts unit-tests/lib/.server/llm/ollama-response-normalization.test.ts`.
- Next exact command: none.
  1. Evaluate assembled executable output quality after synthesis/completion, not only raw model response text.
  2. Derive missing project essentials from normalized/generated artifacts as well as raw artifact text so partial React outputs converge to runnable projects in one loop.
  3. Raise Ollama recovery round budget for smaller models so weaker local outputs get more correction opportunities before fallback.
- Current step now: patch artifact-completion assembly + small-model retry budgets for quality-equivalent convergence.
- Last command run: focused regression suite + preview Playwright passed; full vitest run produced no visible failures in summary sample.
- Next exact command: `pnpm vitest run unit-tests/routes/api.chat.streaming-e2e.test.ts unit-tests/lib/.server/llm/ollama-response-normalization.test.ts unit-tests/lib/runtime/runtime.message-parser.ollama-regressions.test.ts && pnpm tsc --noEmit`
  - `pnpm vitest run unit-tests/utils/projectCommands.test.ts`
- Current step now: loop complete; awaiting next autonomous stabilization objective.
- Last command run: `pnpm vitest run unit-tests/utils/projectCommands.test.ts`.
- Next exact command: `pnpm vitest run unit-tests/routes/api.chat.streaming-e2e.test.ts unit-tests/lib/.server/llm/ollama-response-normalization.test.ts`.

## n8n Workflows (Local Dev Orchestration Only)

- Policy rules:
  - All operational bolt2 workflows in n8n must start with prefix `Project-bolt2-`.
  - Any workflow marked as retired in this file must be removed (or archived fallback) from operational n8n.
  - Run `pnpm run n8n:orchestrator -- prune-retired` whenever retired list changes.

- Name: `Project-bolt2-ongoing-work-dispatch`
  - ID: `FaDMNrIq330kFYpD`
  - Purpose: receives objective events (`next`/`done`/`scan`) and returns next-action guidance for iterative execution.
- Name: `Project-bolt2-ci-publish-watch-sync`
  - ID: `nMxu113Luf2eL5v8`
  - Purpose: orchestrator workflow shell for CI/publish monitoring automation.
- Name: `Project-bolt2-task-orchestrator-queue`
  - ID: `bWliUISoq1GNw2fL`
  - Purpose: Data Table task queue for create/update/complete by taskId and returning next highest-priority open task.

- Active Data Tables (current):
  - Name: `orchestration_tasks`
    - ID: `WwKV0LTmzJj87mm0`
    - Purpose: legacy/shared orchestration task store (`taskId`, `title`, `status`, `priority`, timestamps, AgentID/callback fields).
  - Name: `Project-bolt2-orchestration-tasks`
    - ID: `AOdoRUTVg3zEQCJ9`
    - Purpose: bolt2 orchestration task rows used by queue/orchestration workflow nodes.
  - Name: `Project-bolt2-machine-task-queue`
    - ID: `hGOCfcuUEDEXoRw0`
    - Purpose: machine task push-sync queue and callback delivery status tracking.

- Communication/operation contract:
  - `pnpm run ongoing:bridge -- next` â†’ returns one next objective line for n8n to consume.
  - `pnpm run ongoing:bridge -- prompt` â†’ returns a Copilot-ready prompt built from current unfinished objectives.
  - `pnpm run ongoing:bridge -- json` â†’ returns full machine-readable objective list for n8n branching/filtering.
  - `pnpm run ongoing:bridge:emit` â†’ writes `bolt.work/n8n/copilot-inbox/latest.json` and `bolt.work/n8n/copilot-inbox/latest-prompt.md`.
  - `pnpm run ongoing:cycle -- next` â†’ emits current next objective + notifies n8n, then returns a request message for what to do next.
  - `pnpm run ongoing:cycle -- done "<objective text>"` â†’ records completion, notifies n8n, and returns the next request in queue order.
    - Safety gate: if the objective is currently `PARTIAL`, `done` is blocked unless you pass explicit completion confirmation (`--confirm-complete`).
  - `pnpm run ongoing:cycle -- scan` â†’ checks if unfinished objectives remain and returns final remark to re-check `.ongoing-work.md` when queue is empty.
  - `pnpm run ongoing:cycle:scan:capture` / `pnpm run ongoing:cycle:next:capture` â†’ preferred for automation when JSON capture file is needed (avoids shell redirection writes).
  - `pnpm run ongoing:cycle -- assert` â†’ validates row persistence semantics (`taskStatusTable` active/blocked/completed/placeholder invariants + completed sliding order) before/while iterating tasks.
  - Completed-cycle behavior contract: when queue is drained (`openCount=0`), dispatch response must include `jobPulse: start-new-ongoing-check-job` and `restartCommand: pnpm run ongoing:cycle -- scan` to spawn a fresh follow-up check job.
  - `pnpm run n8n:orchestrator -- list` â†’ shows managed workflow IDs/status.
  - `pnpm run n8n:stats` â†’ reports orchestration metrics (prod executions, failed prod executions, failure rate, average runtime, estimated time saved).
  - `pnpm run n8n:sync-open-tasks` â†’ syncs open objectives to n8n Data Tables when supported; otherwise writes fallback table payload to `bolt.work/n8n/open-tasks-table.json`.
  - `pnpm run n8n:guardrail` â†’ hard check that all operational bolt2 workflows use `Project-bolt2-` prefix.
  - `pnpm run n8n:orchestrator -- activate|deactivate` â†’ manage workflow active states.
  - `pnpm run n8n:orchestrator -- prune-retired` â†’ remove (or archive fallback) retired workflows.
  - `pnpm run test:unit:n8n-live` â†’ env-gated n8n connectivity smoke test (runs live checks only when endpoint+key vars exist).

- Simple orchestration loop (recommended):
  1. n8n schedule/cron runs command: `pnpm run ongoing:bridge:emit`.
  2. n8n reads `bolt.work/n8n/copilot-inbox/latest.json` to route by priority/status.
  3. n8n emits/archives `latest-prompt.md` for the next Copilot execution input.
  4. Copilot run consumes `latest-prompt.md`, executes objective, then updates this file.
  5. Loop repeats until no `PARTIAL`/`TODO` objectives remain.

- Important limitation:
  - n8n cannot directly and autonomously invoke this Copilot chat agent.
  - Retired workflows must be removed/archived from n8n; current retired list includes `overnight-ongoing-work-loop`.
  - Practical pattern: n8n prepares `latest-prompt.md`/`latest.json`; next Copilot run consumes that payload and executes tasks.
  - If webhook route registration fails in the n8n instance, the loop still works with fallback local request messages from `ongoing:cycle` output.
  - Because of the above, the iterative loop is implemented as command-driven handshake (`ongoing:cycle`) plus payload handoff files, not fully autonomous Copilot invocation.
- Env readiness snapshot:
  - `n8n_Arvekari_endpoint`: present.
  - `n8n_Arvekari_ApiKey`: present.
  - Data Tables API on current instance: available (table listing works).
  - Dedicated open-tasks table `Project-bolt2-open-tasks`: not present yet; sync currently falls back to `bolt.work/n8n/open-tasks-table.json` until mapping/creation path is finalized.

## Retired n8n Workflows

- Name: `overnight-ongoing-work-loop`
  - Status: `RETIRED`
  - Operational action: remove/archive from n8n using `pnpm run n8n:orchestrator -- prune-retired`.

## Quick Update Template

- Objective:
- `PARTIAL`:
- `TODO`:
- `BLOCKED`:
- Validation commands run:
- Next exact step:
