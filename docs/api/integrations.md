# API Integrations

Integration behavior is connector-based.

- OpenClaw: optional external connector
- PostgREST: optional external connector
- MCP: adapter-based runtime integration

All optional integrations degrade gracefully if unavailable.
