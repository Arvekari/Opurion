// ChatBox component integration tests (JSX) are in ChatBox.test.tsx.
// This file exists as a non-JSX TypeScript entry for test-mapper compatibility.

import { describe, it, expect } from 'vitest';

describe('app/components/chat/ChatBox.tsx', () => {
  it('has JSX integration tests in ChatBox.test.tsx', () => {
    expect(true).toBe(true);
  });
});
